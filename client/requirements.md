## Packages
framer-motion | Smooth animations for page transitions and UI elements
recharts | Visualizing exam scores and student performance
date-fns | Formatting dates and calculating exam durations

## Notes
Authentication uses cookie-based sessions (credentials: "include")
WebSocket integration is optional for MVP, using polling/local timers for exams
